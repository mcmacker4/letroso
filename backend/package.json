{
  "name": "letroso-backend",
  "version": "0.1.0",
  "description": "Letroso Game",
  "main": "src/main.js",
  "author": "mcmacker4",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec ts-node src/main.ts"
  },
  "dependencies": {
    "koa": "^2.11.0",
    "koa-bodyparser": "^4.2.1",
    "koa-router": "^7.4.0"
  },
  "devDependencies": {
    "@types/koa-bodyparser": "^4.3.0",
    "@types/koa-router": "^7.0.42",
    "typescript": "^3.7.2"
  }
}
